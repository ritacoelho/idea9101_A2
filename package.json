{
  "name": "idea9101_a1",
  "version": "1.0.0",
  "description": "Client-server web app using p5.js and MQTT",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd phone_mqtt_sender && npm install && npm start",
    "server": "cd desktop_mqtt_receiver && npm install && npm start",
    "start": "concurrently \"npm run client\" \"npm run server\""
  },
  "keywords": [
    "p5js",
    "nodejs",
    "mqtt",
    "concurrently"
  ],
  "author": "[Ricky Felix, Maria Rita, Flyn, Aunthika]",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
